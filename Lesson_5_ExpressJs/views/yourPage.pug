extends layout
block content
    h2 Your Tweets
    p There are #{tweets.length} tweets you have

    ol 
        each tweet in tweets
            li 
                a(href=`/tweets/${tweet.id}`) = #{tweet.text}